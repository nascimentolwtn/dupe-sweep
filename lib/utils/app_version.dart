/// Displayed as a small label on the home screen. Kept as a plain constant
/// (rather than reading it from the package via `package_info_plus`) since
/// this is a personal, dependency-light app -- just keep this in sync with
/// the `version:` line in pubspec.yaml when bumping.
const String kAppVersion = '1.4.7';
